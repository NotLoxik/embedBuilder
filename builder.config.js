options = {
    username: 'Discord Bot',
    avatar: 'https://cdn.discordapp.com/embed/avatars/3.png',
    verified: false,
    noUser: false,
    data: null,
    guiTabs: ['author', 'description'],
    useJsonEditor: false,
    reverseColumns: false,
    allowPlaceholders: false,
    autoUpdateURL: false,
    autoParams: false,
    hideEditor: false,
    hidePreview: false,
    hideMenu: false,
    single: false,
    noMultiEmbedsOption: true, // Allow users to have multiple embeds in one message
    sourceOption: false, // Display link to source code in menu.
}

/**
 * Code that will run after page had loaded
 * addEventListener('DOMContentLoaded', () => {
 *   console.log('Hello 👋');
 *
 *   Remove the colour picker
 *   document.querySelector('.colors').remove()
 *  })
 */